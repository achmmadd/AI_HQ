# P1.7 — NUC /motor kiosk-prep

```text
SLICE=motor-shell-kiosk.sh URL-guard only
ACTIVATED=no
DEPLOYED=no
```

Script controleert `http(s)` + pad `/motor` en stopt. Geen browserstart, geen deploy, geen IPs in de repo. Activatie blijft eigenaarshandeling.
