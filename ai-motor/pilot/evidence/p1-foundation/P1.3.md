# P1.3 — Afdelingen + Roster-workbench

> Verdieping van de derde nav-pilaar op de synthetische `/motor`-pagina. Geen commit/push/PR/merge/deploy.
> Repo is publiek: geen echte secrets, IPs of bedrijfsdata.

```text
P1_3_STATUS=BUILT_UNCOMMITTED
BASE_SHA=76253febc2f486491ea07ae056c4411cc70a0862
WORKTREE=/tmp/aihq-p13
WORKTREE_PARENT=/tmp/aihq-p13-src
WORKTREE_CLEAN_AT_START=yes
BRANCH=pilot/p1.3-roster
CHANGED_FILES=6
TEST_COUNT=253 pass / 0 fail / 1 skip
P0_REGRESSION=225 pass / 0 fail / 1 skip (ongewijzigd t.o.v. P1.2-baseline)
P1_TESTS=28 pass (8 foundation + 8 P1.1 + 6 P1.2 + 6 P1.3; delta +6)
TYPES=TYPES_P0=0
LINT=LINT_EXIT=0
P1_CI_COVERAGE=yes (bestaande pilot/*.test.ts-glob; push-branch alleen in worktree)
REAL_CONTEXT_USED=no
EXTERNAL_EFFECTS=no
PUBLIC_INGRESS=no
MASTER_MERGE=no
NEW_DEPENDENCIES=none
POSTGRES_MIGRATION=none
SQLITE=none
READY_FOR=CONTINUE
```

## Fase 0 — inspectie (voor edits)

```text
BASE_SHA=76253febc2f486491ea07ae056c4411cc70a0862
WORKTREE_CLEAN=yes
P1_2_CI=yes (76253fe ci: run Pilot Spine on P1.2 workbench; remote tip van origin/pilot/p1.2-workbench)
CURRENT_DEPARTMENTS=kaartlijst + aanmaakformulier (naam/doel; capabilities hardcoded team.configure); geen detail-workbench; geen update van bestaande afdelingen
CURRENT_ROSTER=gemengde lijst in Afdelingen; Human/AI-badge; identiteit/mandaat/capabilities/projecttoewijzing zichtbaar; geen gescheiden kolommen; geen medewerker↔afdeling
P0_REGRESSION=P0-suite aanwezig; spine-jobs en conformance-real-sidecars ongemoeid
BLOCKED=none
```

`origin/pilot/p1.2-workbench` = `76253febc2f486491ea07ae056c4411cc70a0862` (volledige remote-SHA via `git rev-parse`). Worktree `/tmp/aihq-p13` vanaf verse clone `/tmp/aihq-p13-src`. Niet gewerkt in `/tmp/aihq-integration`.

## Wat gebouwd is

1. **Afdelingen lijst + detail** — bestaande kaarten openen een workbench. Naam, purpose en capabilities zijn configureerbaar binnen `ws-motor`. Legacy-taxonomie (sales/finance/hr/…) blijft DENY.
2. **Roster** — Human en AI Employee in aparte kolommen. Identity, Employee, Agent, Runtime en Model blijven gescheiden begrippen. Mandaat, capabilities en projecttoewijzing zijn zichtbaar; de Employee is geen Agent, Runtime of Model.
3. **Toewijzingen** — medewerker ↔ afdeling ↔ project zijn lokaal/in-memory wijzigbaar. `Herstel lokale sessie` of een nieuwe sessie wist overlay en patches. Geen persistence-laag.
4. **Tenant-isolatie** — `ws-anders` / Noordkaap blijft onzichtbaar via route, query of client-state. Ongeldige workspace of ontbrekende auth faalt gesloten.
5. **Publish** blijft visueel én technisch DENY, ook op de afdeling-workbench na approved review.

Geen nieuwe API, MCP, mail, upload of telefonie. Geen P0-wijziging. De bekende P1.1 UI-typecheck/lint-kloof is bewust niet gedicht.

## Bestanden

Gewijzigd:

- `.github/workflows/pilot-spine.yml` — uitsluitend push-branch `pilot/p1.3-roster`. Jobs, globs, spine en conformance-real-sidecars ongewijzigd.
- `ai-motor/components/p1/p1-foundation-shell.tsx` — Afdelingen-pilaar gebruikt de roster-sessie en de nieuwe workbench.

Nieuw:

- `ai-motor/pilot/p1-roster.ts`
- `ai-motor/pilot/p1-roster.test.ts`
- `ai-motor/components/p1/p1-departments.tsx`
- `ai-motor/pilot/evidence/p1-foundation/P1.3.md`

Geen P0-semantiek, geen gateway/settlement/adapters/MCP/sidecars/store-service/compose, geen .env, geen live-stack.

## Testdelta

P1.2: 247 pass / 0 fail / 1 skip.
P1.3: 253 pass / 0 fail / 1 skip. Delta **+6** in `pilot/p1-roster.test.ts`:

1. afdeling configureren binnen tenant (aanmaken + bestaande bijwerken; legacy-taxonomie DENY)
2. roster Human vs AI begrippen gescheiden (Identity ≠ Employee; geen Agent/Runtime/Model)
3. toewijzing in-memory, niet duurzaam; reset = weg; seed ongewijzigd
4. cross-tenant fail-closed via route, query of client-state
5. publish DENY op de afdeling-workbench na approved review
6. bronnen blijven synthetisch: geen persistence/media/ruwe context

P0-suite blijft 225 pass / 0 fail / 1 skip (skip = alpine-conformance zonder python).

## Validatie (Hetzner-builder, `/tmp/pv-p13`, `node:24-alpine`)

Kopie via rsync van de worktree; geen git push. Geen lokale node/npm.

```text
TESTS_EXIT=0
ℹ pass 253
ℹ fail 0
ℹ skipped 1
TYPES_P0=0
LINT_EXIT=0
```

De bekende P1.1 UI-typecheck/lint-kloof is niet in deze sprint gedicht (owner-besluit). `TYPES_P0=0` dekt de P1.3-modules.

## CI

Bestaande `node --test ... pilot/*.test.ts` dekt `p1-roster.test.ts`. Geen extra teststap, geen versoepeling. Alleen push-branch `pilot/p1.3-roster` toegevoegd in de worktree. `conformance-real-sidecars` ongemoeid. Niet gepusht.

## Grenzen

```text
LIVE_AUTHORITY=NONE
REAL_CONTEXT_USED=no
EXTERNAL_EFFECTS=no
PUBLIC_INGRESS=no
MASTER_MERGE=no
COMMIT=no
PUSH=no
```

## Residuele risico's

1. Geen visuele browser-smoke; bewijs is read-model + bronasserties.
2. Toewijzingen bestaan alleen in de in-memory sessie; herladen wist ze. Bewust: geen persistence-laag.
3. De P1.1 UI-typecheck/lint-kloof blijft open (bewust niet in deze sprint).
4. Workflow-yml-wijziging vereist workflow-scope bij een latere push.
5. Geen commit; wacht op CONTINUE.
