# P1.2 — inbox-filters + project-workbench

> Verdieping van de synthetische `/motor`-ervaring. Geen commit/push/PR/merge/deploy.
> Repo is publiek: geen echte secrets, IPs of bedrijfsdata.

```text
P1_2_STATUS=READY
BASE_SHA=59c3cdff0f444335aa6b49806016d7ac9dc323ce
WORKTREE=/tmp/aihq-p12
WORKTREE_PARENT=/tmp/aihq-p12-src
WORKTREE_CLEAN_AT_START=yes
BRANCH=pilot/p1.2-workbench
CHANGED_FILES=10
TEST_COUNT=247 pass / 0 fail / 1 skip
P0_REGRESSION=225 pass / 0 fail / 1 skip (ongewijzigd t.o.v. P1.1-baseline)
P1_TESTS=22 pass (8 foundation + 8 P1.1 + 6 P1.2; delta +6)
TYPES=TYPES_P0=0; TYPES_APP=2 (pre-existing app/P0-ruis; geen fout in app/motor of components/p1)
LINT=LINT_EXIT=0; LINT_P1_UI=0
P1_CI_COVERAGE=yes
REAL_CONTEXT_USED=no
EXTERNAL_EFFECTS=no
PUBLIC_INGRESS=no
MASTER_MERGE=no
NEW_DEPENDENCIES=none
POSTGRES_MIGRATION=none
SQLITE=none
READY_FOR=COMMIT P1.2
```

## Fase 0 — inspectie (voor edits)

```text
BASE_SHA=59c3cdff0f444335aa6b49806016d7ac9dc323ce
WORKTREE_CLEAN=yes
P1_1_CI=yes (59c3cdf ci: cover P1.1 shell UI — UI-paths + React-typecheck + P1-lint)
CURRENT_INBOX=NowView; vijf aandachtspunten op één project; Open project schakelde alleen naar de projectlijst; geen filters op status/afdeling/eigenaar
CURRENT_PROJECT_VIEW=ProjectCard-lijst met digest/taken/team/artifacts/tijdlijn/draft-review-publish DENY; geen geselecteerde workbench
P0_REGRESSION=P0-suite aanwezig; spine-jobs en conformance-real-sidecars ongemoeid
P1_CI_COVERAGE=paths app/motor + components/p1 + pilot/*.test.ts aanwezig; push-branch pilot/p1.2-workbench ontbrak
BLOCKED=none
```

`origin/pilot/p1.1-shell` = `59c3cdff0f444335aa6b49806016d7ac9dc323ce` (volledige remote-SHA). Descendant van `d050ab3` en `61f2f05`. Worktree `/tmp/aihq-p12` vanaf verse clone `/tmp/aihq-p12-src`. Niet gewerkt in `/tmp/aihq-integration`.

## Wat gebouwd is

1. **Inboxfilters** — status, afdeling en eigenaar. Gefilterde aandachtspunten tonen alleen samenvatting, status, eigenaar/afdeling en bewijsreferenties. Geen contextinhoud, geen ruwe logs.
2. **Projectnavigatie** — elk aandachtspunt opent het bijbehorende tenant-gescope project (`prj-review-keten` of `prj-observatie-keten`). Onbekende of vreemde project-ids falen gesloten.
3. **Project-workbench** — taken, artifacts, drafts, reviews, evidence-referenties en de keten aandacht → project → concept. Review/approved blijft zichtbaar; publish is technisch én visueel DENY, ook na approved.
4. **Lokale toewijzingen** — eigenaar en afdeling van aandachtspunt of project zijn zichtbaar en in-memory wijzigbaar binnen `ws-motor`. `Herstel lokale sessie` of een nieuwe sessie wist de patches. Geen persistence-laag.
5. **Tenant-isolatie** — `ws-anders` / Noordkaap blijft onzichtbaar via route, query of client-state. Ongeldige workspace of ontbrekende auth faalt gesloten.

Tweede synthetische home-project (`Observatie-keten` / Koppelingen) maakt filters en “juiste project” toetsbaar. Geen extra tenant, geen live data.

## Bestanden

Gewijzigd:

- `.github/workflows/pilot-spine.yml` — uitsluitend push-branch `pilot/p1.2-workbench`. Jobs, globs, spine en conformance-real-sidecars ongewijzigd.
- `ai-motor/pilot/p1-foundation.ts` — `ownerId`/`departmentId` op aandachtspunten; tweede home-project met eigen taken/draft/review/publish-DENY.
- `ai-motor/pilot/p1-shell.ts` — getypt draft krijgt owner/department van het project.
- `ai-motor/components/p1/p1-foundation-shell.tsx` — sessie, inbox, workbench-navigatie, herstel lokale sessie.
- `ai-motor/components/p1/ui.tsx` — `P1Select` (native, geen nieuwe npm-dep).

Nieuw:

- `ai-motor/pilot/p1-workbench.ts`
- `ai-motor/pilot/p1-workbench.test.ts`
- `ai-motor/components/p1/p1-inbox.tsx`
- `ai-motor/components/p1/p1-project-workbench.tsx`
- `ai-motor/pilot/evidence/p1-foundation/P1.2.md`

Geen P0-semantiek, geen gateway/settlement/adapters/MCP/sidecars/store-service/compose, geen .env, geen live-stack.

## Testdelta

P1.1: 241 pass / 0 fail / 1 skip.
P1.2: 247 pass / 0 fail / 1 skip. Delta **+6** in `pilot/p1-workbench.test.ts`:

1. inbox filtert op status/afdeling/eigenaar en opent het juiste project
2. andere org onleesbaar via route, query of client-state
3. aandacht → eigen project → concept/review/evidence-refs
4. lokale toewijzing zichtbaar in-sessie, weg na reset; seed ongewijzigd
5. publish DENY op de workbench na approved review (beide home-projecten)
6. bronnen blijven synthetisch: geen persistence/media/ruwe context

P0-suite blijft 225 pass / 0 fail / 1 skip (skip = alpine-conformance zonder python).

## Validatie (Hetzner-builder, `/tmp/pv-p12`, `node:24-alpine`)

Kopie via rsync van de worktree; geen git push. Geen lokale node/npm.

```text
TESTS_EXIT=0
ℹ pass 247
ℹ fail 0
ℹ skipped 1
TYPES_P0=0
TYPES_APP=2
LINT_EXIT=0
LINT_P1_UI=0
```

`TYPES_APP=2` is pre-existing app/P0-ruis (`lib/photo-studio`, `pilot/conformance.test.ts`, `pilot/server.ts`). Niet gefixt; P0 niet gewijzigd. Geen nieuwe fout in `app/motor` of `components/p1`. `TYPES_P0=0` dekt de P1.2-modules.

## CI

Bestaande `node --test ... pilot/*.test.ts` dekt `p1-workbench.test.ts`. Geen extra teststap, geen versoepeling. Alleen push-branch `pilot/p1.2-workbench` toegevoegd. `conformance-real-sidecars` ongemoeid.

## Grenzen

```text
LIVE_AUTHORITY=NONE
REAL_CONTEXT_USED=no
EXTERNAL_EFFECTS=no
PUBLIC_INGRESS=no
MASTER_MERGE=no
COMMIT=no
PUSH=no
```

## Residuele risico's

1. Uncommitted tot ownerratificatie. `READY_FOR=COMMIT P1.2`.
2. Geen visuele browser-smoke; bewijs is read-model + bronasserties.
3. Toewijzingen bestaan alleen in de in-memory sessie; herladen wist ze. Bewust: geen persistence-laag.
4. `TYPES_APP=2` blijft pre-existing app/P0-ruis.
5. Workflow-yml-wijziging vereist workflow-scope bij een latere push.

Wacht op `COMMIT P1.2`. Daarna apart `PUSH P1.2`.
