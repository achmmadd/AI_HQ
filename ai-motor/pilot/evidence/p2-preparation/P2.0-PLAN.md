# P2.0-PLAN — tailnet `/motor` + NUC-kiosk

> **Status:** SERVICE LIVE — KIOSK BLOCKED (geen display / X)  
> `/motor` is bereikbaar vanaf de NUC. Chromium kan niet op het scherm
> zonder monitor + minimale X-stack. P0 `:4400` ongemoeid.  
> **Afsluiting:** [`P2-COMPLETE.md`](P2-COMPLETE.md). Live-feiten: [`P2.0-ACTIVATE.md`](P2.0-ACTIVATE.md).

```text
BASE_SHA=0986463f0bad92e4709aa37729ea17dc1331374e
P2_PREP_SHA=UNCOMMITTED
TARGET_TOPOLOGY=Hetzner-only Motor-server; NUC = kiosk/browser naar één gepinde tailnet-origin
EXACT_TAILNET_ORIGIN=http://100.97.30.22:4420/motor
ORIGIN_CHOICE=A
MOTOR_SERVICE=motor-p2-ui (pilot/p2-motor-server.ts) op 100.97.30.22:4420; P2_REPO_PATH + PILOT_P2_ACL; niet de P0-API op :4400
NUC_KIOSK_POLICY=alleen http://100.97.30.22:4420/motor; probe /motor/health; geen backend/proxy/secrets
PILOT_ACL_DELTA={"nkcmY58XEw11CNTRL":["ws-motor"]}
VOLUMES_AND_SECRETS=geen nieuwe volumes; geen nieuwe secrets; bestaande P0-volumes ongemoeid
BROWSER_SMOKES=synthetisch in p2-motor.test.ts + p2-kiosk.test.ts
CI_GATES=bestaande spine + conformance pikt pilot/*.test.ts op; UI-tsc/lint-remedie niet uitgevoerd
ROLLBACK=alleen motor-p2-ui stoppen; nooit docker compose down
PERSISTENCE_ADR_DECISION=P2.1 only; SQLite forbidden
REAL_CONTEXT_USED=no
EXTERNAL_EFFECTS=no
PUBLIC_INGRESS=no
MASTER_MERGE=no
ACTIVATION_REQUIRES=explicit owner command ACTIVATE P2.0
NUC_SSH=motorai-server2 / 100.123.185.0 / motorai2; StableID=nkcmY58XEw11CNTRL
NUC_IDENTITY_SOURCE=NUC-SSH-chat read-only; Self.StableID empty; Self.ID used
ACL_APPLIED=no
KIOSK_RUNNING=no
NUC_BROWSER=none
NUC_PROBE_TOOL=curl
NUC_SCRIPT_PRESENT=no
NUC_HEALTH_HTTP=000
NUC_HEALTH_REACHABLE=no
```

## Ownerbeslissingen (2026-08-15)

| Veld | Waarde |
|---|---|
| `ORIGIN_CHOICE` | **A** — `http://100.97.30.22:4420/motor` |
| `PILOT_ACL_DELTA` | **exact, niet toegepast:** `{"nkcmY58XEw11CNTRL":["ws-motor"]}` |
| `PLAN P2.0 APPROVED` | **yes** |
| Activatie | P2.0 gedaan (2026-08-15); kiosk geblokkeerd; P2.1 journal niet live |

NUC-identiteit (read-only, NUC-SSH-chat, 2026-08-15):

```text
NUC_HOSTNAME=motorai-server2
NUC_TAILNET_IP=100.123.185.0
NUC_STABLE_ID=nkcmY58XEw11CNTRL
KIOSK_RUNNING=no
LOCAL_MOTOR_BACKEND=no
LOCAL_DOCKER=no
```

Exacte delta (StableID, nooit node-naam). **Niet** live gezet:

```json
{"nkcmY58XEw11CNTRL":["ws-motor"]}
```

Toepassen op Hetzner vereist een expliciete eigenaarsopdracht
(`APPLY PILOT_ACL_DELTA` of als onderdeel van `ACTIVATE P2.0`).
Geen extra nodes, geen `motorai-nuc` als key, geen extra workspaces.

## NUC kiosk-readiness (read-only, 2026-08-15)

```text
NUC_HOSTNAME=motorai-server2
NUC_USER=motorai2
NUC_TAILNET_IP=100.123.185.0
NUC_STABLE_ID=nkcmY58XEw11CNTRL
LOCAL_MOTOR_BACKEND=no
LOCAL_DOCKER=no
KIOSK_RUNNING=no
BROWSER=none
PROBE_TOOL=curl
SCRIPT_PRESENT=no
HEALTH_HTTP=000
HEALTH_REACHABLE=no
```

Verwacht: health 000 omdat `motor-p2-ui` niet live is; script ontbreekt
omdat de launcher nog niet naar de NUC is gekopieerd.

**NUC-gat vóór kioskstart:** geen Chromium/Firefox. `ACTIVATE P2.0` mag
de Hetzner-dienst + ACL doen, maar de browser-kiosk faalt tot de eigenaar
apart een browsertoestemming geeft. Geen stille `apt install`.

## Geverifieerde baseline (read-only, 2026-08-15)

| Claim | Bevinding |
|---|---|
| P1-remote | `origin/pilot/p1-continuous` = `0986463f0bad92e4709aa37729ea17dc1331374e` |
| Live Hetzner-repo | `fc37d90c853b4e1760aae7770b14020cf2d9b18e` |
| Bind P0 | `100.97.30.22:4400` (niet loopback) |
| HTTP P0 | `/` 200; `/motor` 404 |
| Context | `CONTEXT_MODE=demo` |
| Publieke host-poorten | 443/8443 = andere workloads. Niet aanraken. |
| NUC in ACL | nee |

## Gebouwde prep (lokaal, niet live)

| Pad | Rol |
|---|---|
| `pilot/p2-motor-server.ts` | Minimale `/motor` + `/api/motor/*`; WhoIs + ACL; in-memory P1 |
| `pilot/p2-bind.ts` | Weigert `0.0.0.0`; default `127.0.0.1:4420` |
| `pilot/p2-kiosk-policy.ts` | Alleen de gepinde origin |
| `infra/pilot/compose.p2-motor.yaml` | Overlay `--no-deps motor-p2-ui` |
| `infra/pilot/nuc/motor-p2-kiosk.sh` | Pin + health-probe; geen secrets |

P0 `/`, `/draft`, `/decision`, settlement, MCP, adapters en DENY blijven
ongewijzigd. Geen persistence, geen SQLite, geen master-merge.

## CI_GATES

Bestaande `pilot-spine.yml` draait `pilot/*.test.ts` — P2.0-tests vallen
daaronder zodra de branch in CI komt. Workflow is **niet** gewijzigd
(geen `pilot/p2.0-motor-kiosk` in de push-branchlijst; PR-paden dekken
`ai-motor/pilot/**`).

Minimale UI-remedie (nog steeds aparte toestemming):

```yaml
- run: ./node_modules/.bin/tsc --noEmit -p tsconfig.p1.json
- run: ./node_modules/.bin/eslint app/motor components/p1 --max-warnings 0
```

## Rollback

```text
docker compose -f compose.yaml -f compose.p2-motor.yaml stop motor-p2-ui
# nooit: docker compose down
```

## Residual risks

```text
RESIDUAL_RISKS=whois-bind; live-sha-skew; role-split-not-live; journal-not-live; CI-push-deferred
PLAN_STATUS=P2.0 LIVE — KIOSK BLOCKED — P2 CLOSED_SYNTHETIC
```

P2.0-console is live; kiosk is het verkeerde doel. Journal, role-split-ACL
en GitHub-push blijven later. Zie `P2-COMPLETE.md`.
