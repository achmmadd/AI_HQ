# CONTINUE P2 — role split in code (verbeterde prompt, uitgevoerd)

```text
WHERE=pilot/p2.0-motor-kiosk after orchestrator foundation
CI_PUSH=Codex track; do not block
LIVE=no deploy, no .env, no NUC copy, no ACL mutate
```

## Contract

- Laptop in `PILOT_P2_ACL` → `/motor` + review/publish-DENY.
- NUC `nkcmY58XEw11CNTRL` → alleen `/motor/orchestrator/health|ready`.
- Die NUC-id is **nooit** UI, ook niet als hij nog in `PILOT_P2_ACL` staat.
- Alleen die NUC-id mag orchestrator, ook als een andere id in de orchestrator-ACL staat.
- WhoIs StableID only. Geen hostname/IP/headers.

Geen live activatie in deze slice.
