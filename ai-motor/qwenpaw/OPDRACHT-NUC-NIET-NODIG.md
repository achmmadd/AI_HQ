# Kort bericht aan QwenPaw — NUC niet nodig

> Plak dit in de bestaande `boka_operations`-chat als de agent nog wacht op de NUC. Als de skill-bestanden er nog niet staan, plak daarna `OPDRACHT-VERVOLG.md` in zijn geheel.

---

Pietje hier. Bevestiging: **de NUC is niet nodig.** Jij bent de live instance (Docker, agent `boka_operations`). Wacht niet, vraag geen NUC-toegang, meet niet of je host de NUC is.

Ga verder met schrijven van skill + persona (als dat nog niet gebeurd is), enable `project-administratie`, daarna `motor_admin.py probe` en `status`. `OFFLINE` is geldig. Geen token, geen verzonnen URL, `agent.json` niet overschrijven.

Daarna ben je in dienst.
