# Omega AI-Holding / Singularity — kopieer naar .env en vul in. Commit .env nooit.
# cp .env.example .env   of: ./scripts/create_env_if_missing.sh

# ——— Telegram & AI (bestaand) ———
TELEGRAM_BOT_TOKEN=123456789:AAFxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_API_KEY=
# OPENAI_API_KEY=
# OLLAMA_MODEL=llama3.2:3b
# GEMINI_MODEL=gemini-2.0-flash

# ——— MCP & externe tools (Singularity) ———
# Brave Search (market trends, proactieve scan)
# BRAVE_SEARCH_API_KEY=
# GitHub (repos, issues)
# GITHUB_TOKEN=
# Google Workspace (Drive, Sheets, Gmail) — OAuth of service account
# GOOGLE_WORKSPACE_CREDENTIALS_JSON=
# Filesystem MCP: lokaal, geen key nodig

# ——— State & geheugen ———
# ChromaDB (lokaal) of Pinecone (cloud) voor holding-geheugen
# CHROMADB_PERSIST_DIR=./data/chromadb
# PINECONE_API_KEY=
# PINECONE_INDEX=

# ——— Security & netwerk ———
# Tailscale (zero-trust toegang tot NUC/action layer)
# TAILSCALE_AUTH_KEY=
# TAILSCALE_TAILNET=

# ——— Multimodal / assets ———
# Midjourney of Flux API (auto-asset uit Brand DNA)
# MIDJOURNEY_API_KEY=
# FLUX_API_KEY=
# REPLICATE_API_TOKEN=

# ——— 1Panel (na ./scripts/install_1panel_bridge.sh) ———
# ONEPANEL_BASE_URL=http://IP:port
# ONEPANEL_API_KEY=
# (PANEL_API_KEY = zelfde als ONEPANEL_API_KEY, voor compatibiliteit)

# ——— Cloudflare Zero-Trust (Omega Global: Secure Singularity) ———
# TUNNEL_TOKEN=   (uit Cloudflare Zero Trust → Tunnels → Create → token)
# Start tunnel: docker compose --profile with-tunnel up -d
# ALLOWED_IPS=   (optioneel; komma-gescheiden IP's voor allowlist; X-Forwarded-For / CF-Connecting-IP vertrouwen in 1Panel)

# ——— Optioneel ———
# TELEGRAM_CHAT_ID=   (voor log-alerts, Engineer auto-repair, lockdown meldingen)
# CANVA_API_KEY=
# E2EE: voor versleutelde rapporten naar Telegram (eigen key pair)
# OMEGA_REPORT_ENCRYPTION_KEY=
