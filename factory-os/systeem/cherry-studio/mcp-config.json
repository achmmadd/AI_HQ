{
  "mcpServers": {
    "factory-os-memory": {
      "name": "Factory OS — Geheugen",
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "description": "Persistent geheugen tussen Cherry Studio sessies (lokaal op Mac)"
    },
    "factory-os-fetch": {
      "name": "Factory OS — Web Fetch",
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "mcp-fetch-server"
      ],
      "description": "URLs ophalen — o.a. NUC-services via Tailscale-IP"
    }
  }
}
